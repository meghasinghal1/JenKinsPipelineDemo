pipeline {
    agent any

    stages {
        stage('zipRepo') {
	    steps {
	    	script{
	         	zip archive: true, dir: 'sast-web-test-1', glob: '', zipFile: 'abcccccc.zip'
		 }
	    }
        }
    }
}
