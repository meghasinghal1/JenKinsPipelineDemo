pipeline {
    agent any 
    stages{
        stage('test'){
            steps{
                sh 'mkdir -p archive'
                sh 'echo test > archive/test.txt'
            }
        }
    }
}
