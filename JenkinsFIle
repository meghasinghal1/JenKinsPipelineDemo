pipeline {
    agent any 
    stages{
        stage('test'){
            steps{
                echo 'testtestiing'
                script{
                    zip archive: true, dir: 'archive', glob: '', zipFile: 'coverage-files.zip'
                }
            }
        }
    }
}
