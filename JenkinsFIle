pipeline {
    agent any

    stages {
        stage('zipRepo') {
            steps {
                script {
                    // Define a reusable function to run PowerShell commands
                    def runPowerShell = { command ->
                        try {
                            // Attempt to use PowerShell Core (pwsh)
                            pwsh command
                        } catch (Exception e) {
                            // If pwsh fails, use Windows PowerShell
                            echo "\u001B[33mFailed to run pwsh: ${e.message}, falling back to Windows PowerShell.\u001B[0m"
                            powershell command
                        }
                    }

                    // Call the function with the desired command
                    runPowerShell '''
                        Write-Output "Zipping completed successfully!" | Out-Host
                    '''
                }
            }
        }
    }
}
