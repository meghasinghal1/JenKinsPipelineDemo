pipeline {
    agent any 
    stages{
        stage('test'){
            steps{
                echo 'testtestiing'
                dir('WebGoat') {
                    sh '''
                    zip -r  WebGoat.zip algo
                    '''
               }
            }
        }
    }
}
